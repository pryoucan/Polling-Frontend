# Base URL of the backend API. Copy to .env (or .env.local) and adjust.
# Local dev (backend on your machine):
VITE_API_BASE=http://localhost:8080
# Production (set in Vercel project env vars) — your AWS backend URL, e.g.:
# VITE_API_BASE=https://api.yourdomain.com
